---
import BaseLayout from "../layouts/BaseLayout.astro";
import { getCollection } from "astro:content";
import { withBase } from "../utils/paths";

const chapters = (await getCollection("tutorial")).sort((a, b) => a.data.order - b.data.order);
const coreConcepts = chapters.find((entry) => entry.slug === "core-concepts");
const textToImage = chapters.find((entry) => entry.slug === "text-to-image");
const workflowReuse = chapters.find((entry) => entry.slug === "workflow-reuse");
const resolveUrl = (value?: string | null) => withBase(value) ?? value ?? undefined;
---
<BaseLayout>
  <section class="rounded-3xl bg-gradient-to-br from-primary/10 via-surface-light to-muted-light p-10 text-slate-800 shadow-lg ring-1 ring-primary/20 dark:from-primary/15 dark:via-surface-dark dark:to-muted-dark dark:text-slate-100">
    <div class="max-w-3xl space-y-6">
      <p class="text-sm uppercase tracking-[0.35em] text-primary">ComfyUI 快速上手</p>
      <h1 class="text-4xl font-bold sm:text-5xl">从核心概念到常用工作流，15 分钟建立你的 ComfyUI 直觉</h1>
      <p class="text-lg text-slate-600 dark:text-slate-300">
        本教程专注于最常用、最实用的 ComfyUI 工作流。通过三大章节，你将理解节点如何协同、亲手搭建文本到图片流水线，并学会复用社区工作流文件。
      </p>
      <div class="flex flex-wrap gap-3 text-sm">
        <a class="rounded-md bg-primary px-4 py-2 font-semibold text-white shadow-lg shadow-primary/30 transition hover:bg-primary-dark" href="#text-to-image">直接开始动手</a>
        <a class="rounded-md border border-primary px-4 py-2 font-semibold text-primary transition hover:bg-primary/10" href="#concepts">先熟悉核心概念</a>
      </div>
    </div>
  </section>

  <section id="concepts" class="mt-16 space-y-6">
    <header class="flex flex-col gap-2">
      <p class="text-xs font-semibold uppercase tracking-[0.3em] text-primary">章节 01</p>
      <h2 class="text-3xl font-bold">核心概念解析：用积木搭起图像生成链路</h2>
      <p class="text-slate-600 dark:text-slate-300">
        以最贴近实际操作的比喻解释节点、工作流、Checkpoint、LoRA、VAE，让你在开始操作前先建立逻辑模型。
      </p>
    </header>
    {coreConcepts ? (
      <article class="grid gap-8 rounded-2xl border border-slate-200 bg-white/80 p-8 shadow-sm backdrop-blur dark:border-slate-700 dark:bg-slate-900/70 md:grid-cols-[minmax(0,1.4fr)_minmax(0,1fr)]">
        {coreConcepts.data.heroImage && (
          <img src={resolveUrl(coreConcepts.data.heroImage)} alt="核心概念示意图" class="rounded-xl shadow-lg ring-1 ring-primary/20" loading="lazy" />
        )}
        <div class="flex flex-col gap-4">
          <p class="text-sm leading-relaxed text-slate-600 dark:text-slate-300">{coreConcepts.data.description}</p>
          <ul class="flex flex-wrap gap-2 text-xs text-primary">
            {coreConcepts.data.keywords?.map((keyword) => (
              <li class="rounded-full border border-primary/40 px-3 py-1">{keyword}</li>
            ))}
          </ul>
          <div class="mt-auto flex flex-wrap gap-4 text-sm">
            <a class="inline-flex items-center gap-2 rounded-md border border-primary px-4 py-2 font-semibold text-primary transition hover:bg-primary/10" href={resolveUrl(`tutorial/${coreConcepts.slug}/`)}>
              阅读完整章节
            </a>
          </div>
        </div>
      </article>
    ) : (
      <div class="callout">章节内容尚未准备好，稍后再来看。</div>
    )}
  </section>

  <section id="text-to-image" class="mt-24 space-y-6">
    <header class="flex flex-col gap-2">
      <p class="text-xs font-semibold uppercase tracking-[0.3em] text-primary">章节 02</p>
      <h2 class="text-3xl font-bold">文生图实战：15 分钟搭起第一条工作流</h2>
      <p class="text-slate-600 dark:text-slate-300">
        用最少节点覆盖完整的文本到图片流程，每一步都配有红色标注与动图演示，确保 100% 可复现。
      </p>
    </header>
    {textToImage ? (
      <article class="grid gap-8 rounded-2xl border border-slate-200 bg-white/80 p-8 shadow-sm backdrop-blur dark:border-slate-700 dark:bg-slate-900/70 md:grid-cols-[minmax(0,1fr)_minmax(0,1fr)]">
        <div class="flex flex-col gap-4">
          <p class="text-sm leading-relaxed text-slate-600 dark:text-slate-300">{textToImage.data.description}</p>
          <ul class="flex flex-wrap gap-2 text-xs text-primary">
            {textToImage.data.keywords?.map((keyword) => (
              <li class="rounded-full border border-primary/40 px-3 py-1">{keyword}</li>
            ))}
          </ul>
          <div class="mt-auto flex flex-wrap gap-4 text-sm">
            <a class="inline-flex items-center gap-2 rounded-md border border-primary px-4 py-2 font-semibold text-primary transition hover:bg-primary/10" href={resolveUrl(`tutorial/${textToImage.slug}/`)}>
              查看动图演示
            </a>
            <a class="inline-flex items-center gap-2 rounded-md border border-primary/40 px-4 py-2 font-semibold text-primary transition hover:bg-primary/10" href={resolveUrl("assets/workflows/text-to-image-basic.json")} download>
              下载示例工作流
            </a>
          </div>
        </div>
        {textToImage.data.gifDemo ? (
          <img src={resolveUrl(textToImage.data.gifDemo)} alt="文生图工作流动图" class="w-full rounded-xl border border-primary/20 shadow-lg" loading="lazy" />
        ) : (
          textToImage.data.heroImage && (
            <img src={resolveUrl(textToImage.data.heroImage)} alt="文生图节点示意图" class="w-full rounded-xl shadow-lg" loading="lazy" />
          )
        )}
      </article>
    ) : (
      <div class="callout">章节内容尚未准备好，稍后更新。</div>
    )}
  </section>

  <section id="workflow-reuse" class="mt-24 space-y-6">
    <header class="flex flex-col gap-2">
      <p class="text-xs font-semibold uppercase tracking-[0.3em] text-primary">章节 03</p>
      <h2 class="text-3xl font-bold">工作流复用：用好社区资源并个性化改造</h2>
      <p class="text-slate-600 dark:text-slate-300">
        学会加载别人分享的 JSON 工作流，快速定位关键节点并替换模型、提示词，把优秀方案变成自己的常备模板。
      </p>
    </header>
    {workflowReuse ? (
      <article class="grid gap-8 rounded-2xl border border-slate-200 bg-white/80 p-8 shadow-sm backdrop-blur dark:border-slate-700 dark:bg-slate-900/70 md:grid-cols-[minmax(0,1fr)_minmax(0,1fr)]">
        {workflowReuse.data.gifDemo ? (
          <img src={resolveUrl(workflowReuse.data.gifDemo)} alt="工作流复用演示动图" class="w-full rounded-xl border border-primary/20 shadow-lg" loading="lazy" />
        ) : (
          workflowReuse.data.heroImage && (
            <img src={resolveUrl(workflowReuse.data.heroImage)} alt="工作流复用示意图" class="w-full rounded-xl shadow-lg" loading="lazy" />
          )
        )}
        <div class="flex flex-col gap-4">
          <p class="text-sm leading-relaxed text-slate-600 dark:text-slate-300">{workflowReuse.data.description}</p>
          <ul class="flex flex-wrap gap-2 text-xs text-primary">
            {workflowReuse.data.keywords?.map((keyword) => (
              <li class="rounded-full border border-primary/40 px-3 py-1">{keyword}</li>
            ))}
          </ul>
          <div class="mt-auto flex flex-wrap gap-4 text-sm">
            <a class="inline-flex items-center gap-2 rounded-md border border-primary px-4 py-2 font-semibold text-primary transition hover:bg-primary/10" href={resolveUrl(`tutorial/${workflowReuse.slug}/`)}>
              阅读改造策略
            </a>
            <a class="inline-flex items-center gap-2 rounded-md border border-primary/40 px-4 py-2 font-semibold text-primary transition hover:bg-primary/10" href={resolveUrl("assets/workflows/workflow-reuse-template.json")} download>
              下载复用模板
            </a>
          </div>
        </div>
      </article>
    ) : (
      <div class="callout">章节内容即将上线。</div>
    )}
  </section>

  <section id="downloads" class="mt-24 rounded-3xl border border-primary/30 bg-primary/5 p-10 shadow-lg dark:border-primary/40 dark:bg-primary/10">
    <h2 class="text-2xl font-bold">配套资源下载</h2>
    <p class="mt-2 text-sm text-slate-600 dark:text-slate-300">所有示例工作流均经过实机验证，可直接导入 ComfyUI。</p>
    <ul class="mt-6 space-y-3 text-sm">
      <li><a class="inline-flex items-center gap-2 rounded-md bg-white px-4 py-2 font-semibold text-primary shadow-sm ring-1 ring-primary/30 transition hover:bg-primary/10 dark:bg-slate-900" href={resolveUrl("assets/workflows/text-to-image-basic.json")} download>基础文生图工作流 JSON</a></li>
      <li><a class="inline-flex items-center gap-2 rounded-md bg-white px-4 py-2 font-semibold text-primary shadow-sm ring-1 ring-primary/30 transition hover:bg-primary/10 dark:bg-slate-900" href={resolveUrl("assets/workflows/workflow-reuse-template.json")} download>工作流复用模板 JSON</a></li>
    </ul>
  </section>
</BaseLayout>

